"""
Maybe worth having a wrapper for sklearn's NearestNeighbors and pass custom metrics.
The goal is,
- Here's my training data
- Find me the objects which are "close" to this data.

- Maybe have a wrapper that does this as an aggregate of all (unique) metrics.

TBC

"""
